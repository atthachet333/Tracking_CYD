/* ============================================================
   Current user config กลาง (แสดงมุมขวาบน)
   - avatarUrl = รูปผู้ใช้จริงเดิม (ไม่ใช้โลโก้แทน avatar)
   ============================================================ */
export const CURRENT_USER = {
  displayName: "Executive",
  roleLabel: "ผู้บริหาร",
  avatarUrl: "/executive.png",
} as const;
