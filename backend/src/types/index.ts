// Re-export domain types จาก shared สำหรับใช้ภายใน backend
export type {
  Employee, Department, OrgNode, Task, DocumentItem, Approval, Customer,
  ServiceType, NotificationItem, Insight, DashboardSummary, TrendPoint,
  ReportData, Paginated,
} from "@tracking-cyd/shared";
